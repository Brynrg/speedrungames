[build]
  base    = "."
  command = "pnpm install --frozen-lockfile && pnpm -C apps/web build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION            = "22"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
